<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gov.alaska.dggs.igneous.MiningDistrict">
	<select id="getList" resultSetType="FORWARD_ONLY" resultMap="MiningDistrictMap">
		SELECT mining_district_id, name,
			ST_AsGeoJson(ST_Simplify(geog::GEOMETRY, 0.001)::GEOGRAPHY, 3) AS geojson
		FROM mining_district
		ORDER BY LOWER(name)
	</select>


	<select id="getByProspectID" resultSetType="FORWARD_ONLY" parameterType="long" resultMap="MiningDistrictMap">
		SELECT DISTINCT md.mining_district_id, md.name
		FROM borehole AS bh
		JOIN borehole_geog AS bg
			ON bg.borehole_id = bh.borehole_id
		JOIN mining_district AS md
			ON ST_Intersects(md.geog, bg.geog)
		WHERE bh.prospect_id = #{prospect_id}
	</select>


	<select id="getByBoreholeID" resultSetType="FORWARD_ONLY" parameterType="long" resultMap="MiningDistrictMap">
		SELECT DISTINCT md.mining_district_id, md.name
		FROM borehole_geog AS bg
		JOIN mining_district AS md
			ON ST_Intersects(md.geog, bg.geog)
		WHERE bg.borehole_id = #{borehole_id}
	</select>


	<resultMap id="MiningDistrictMap" type="MiningDistrict">
		<id property="id" column="mining_district_id" />
		<result property="name" column="name" />
		<result property="geojson" column="geojson" />
	</resultMap>
</mapper>
