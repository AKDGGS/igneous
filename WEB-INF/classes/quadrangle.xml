<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gov.alaska.dggs.igneous.Quadrangle">
	<select id="getByProspectID" resultSetType="FORWARD_ONLY" parameterType="long" resultMap="QuadrangleMap">
		SELECT DISTINCT
			qr.quadrangle_id, qr.name, qr.alt_name,
			qr.abbr, qr.alt_abbr, qr.scale
		FROM borehole AS bh
		JOIN borehole_geog AS bg ON bg.borehole_id = bh.borehole_id
		JOIN quadrangle AS qr ON ST_Intersects(qr.geog, bg.geog)
		WHERE bh.prospect_id = #{id} AND qr.scale = 250000
	</select>


	<resultMap id="QuadrangleMap" type="Quadrangle">
		<id property="id" column="quadrangle_id" />
		<result property="name" column="name" />
		<result property="alt_name" column="alt_name" />
		<result property="abbr" column="abbr" />
		<result property="alt_abbr" column="alt_abbr" />
		<result property="scale" column="scale" />
	</resultMap>
</mapper>
