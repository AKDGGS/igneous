<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gov.alaska.dggs.igneous.Quadrangle">
	<select id="getList" resultSetType="FORWARD_ONLY" resultMap="QuadrangleMap">
		SELECT qr.quadrangle_id, qr.name,
			ST_AsText(ST_Simplify(ST_Transform(geog::GEOMETRY, 3857), 5000)) AS wkt
		FROM quadrangle AS qr
		WHERE qr.scale = 250000
		ORDER BY name
	</select>


	<sql id="getBy">
		SELECT DISTINCT qr.quadrangle_id, qr.name, qr.alt_name,
			qr.abbr, qr.alt_abbr, qr.scale
	</sql>


	<select id="getByProspectID" resultSetType="FORWARD_ONLY" parameterType="long" resultMap="QuadrangleMap">
		<include refid="getBy" />
		FROM borehole AS bh
		JOIN borehole_geog AS bg ON bg.borehole_id = bh.borehole_id
		JOIN quadrangle AS qr ON ST_Intersects(qr.geog, bg.geog)
		WHERE bh.prospect_id = #{prospect_id}
			AND qr.scale = 250000
	</select>


	<select id="getByBoreholeID" resultSetType="FORWARD_ONLY" parameterType="long" resultMap="QuadrangleMap">
		<include refid="getBy" />
		FROM borehole_geog AS bg
		JOIN quadrangle AS qr ON ST_Intersects(qr.geog, bg.geog)
		WHERE bg.borehole_id = #{borehole_id}
			AND qr.scale = 250000
	</select>


	<select id="getByWellID" resultSetType="FORWARD_ONLY" parameterType="long" resultMap="QuadrangleMap">
		<include refid="getBy" />
		FROM well_geog AS wg
		JOIN quadrangle AS qr ON ST_Intersects(qr.geog, wg.geog)
		WHERE wg.well_id = #{well_id}
			AND qr.scale = 250000
	</select>


	<resultMap id="QuadrangleMap" type="Quadrangle">
		<id property="id" column="quadrangle_id" />
		<result property="name" column="name" />
		<result property="alt_name" column="alt_name" />
		<result property="abbr" column="abbr" />
		<result property="alt_abbr" column="alt_abbr" />
		<result property="scale" column="scale" />
	</resultMap>
</mapper>
