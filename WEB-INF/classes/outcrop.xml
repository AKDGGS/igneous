<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gov.alaska.dggs.igneous.Outcrop">
	<sql id="getBy">
		SELECT o.outcrop_id, o.name,
			o.outcrop_number, o.is_onshore, o.year
	</sql>


	<select id="getByID" resultSetType="FORWARD_ONLY" parameterType="long" resultMap="OutcropMap">
		<include refid="getBy" />
		FROM outcrop AS o
		WHERE o.outcrop_id = #{outcrop_id}
	</select>


	<select id="getByInventoryID" resultSetType="FORWARD_ONLY" parameterType="long" resultMap="OutcropMap">
		<include refid="getBy" />
		FROM inventory_outcrop AS io
		JOIN outcrop AS o
			ON o.outcrop_id = io.outcrop_id
		WHERE io.inventory_id = #{inventory_id}
	</select>


	<resultMap id="OutcropMap" type="Outcrop">
		<id property="id" column="outcrop_id" />

		<result property="name" column="name" />
		<result property="number" column="outcrop_number" />
		<result property="is_onshore" column="is_onshore" />
		<result property="year" column="year" />
	</resultMap>


	<select id="getIDByWKT" resultSetType="FORWARD_ONLY" resultType="int">
		SELECT DISTINCT outcrop_id FROM outcrop_geog
		WHERE ST_Intersects(geog, ST_Transform(ST_GeomFromText(#{wkt}, 3857), 4326))
	</select>
</mapper>
